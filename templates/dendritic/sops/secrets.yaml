# PLACEHOLDER — this file is NOT encrypted yet.
#
# Replace it before your first switch:
#   1. put real age recipients in sops/.sops.yaml
#   2. sops sops/secrets.yaml     (edits + encrypts in place)
#   3. flip validateSopsFiles back to true in modules/system/sops.nix
#
# Keys referenced by the template:
passwords:
  # mkpasswd -m sha-512
  example: "$6$REPLACE$ME"
private_keys:
  # contents of ~/.ssh/id_ed25519 for the example user
  example: |
    -----BEGIN OPENSSH PRIVATE KEY-----
    REPLACE ME
    -----END OPENSSH PRIVATE KEY-----
api_keys:
  openai: REPLACE_ME
