# Zsh shell configuration
#
# Zsh itself is configured via stow (home/.config/zsh/).
# This module is a placeholder for future home-manager zsh integration.
_: {
  flake.modules.homeManager.zsh = _: { };
}
